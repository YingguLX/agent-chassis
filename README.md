# agent-chassis
Agent Chassis is a constraint-engineering template for AI-assisted development. Six version-locked documents replace the monolithic prompt. Trigger-command chains enforce serial quality gates; anti-weakening guards prevent rule dilution; explore/general subagent separation blocks unauthorized writes. Fill placeholders, instantiate, enforce.
